# Agent Instructions

## Build / Run

- Install dependencies with `npm install`.
- Run local checks with `npm test`.

## Test / Verification

- Run `npm run lint` before changing JavaScript files.
- Run `npm run docs:check` after editing Markdown.

## Constraints

- Keep npm script names stable because AGENTS.md references them directly.
- Do not add generated build output to the example.
