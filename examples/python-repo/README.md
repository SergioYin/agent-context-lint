# Python Example Repo

Small package-style fixture used to demonstrate metadata-aware command matching.

```bash
python -m pip install -e .
python -m pytest -q
python -m demo_agent_pkg
agent-demo --help
```
