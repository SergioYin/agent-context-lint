# Agent Instructions

## Docs Commands

- Check links with `python scripts/check_links.py docs`.
- Check Markdown structure with `python scripts/check_markdown.py docs`.

## Constraints

- Keep headings stable unless the table of contents is updated.
- Prefer plain Markdown over generated documentation artifacts.

## Verification

- Run both docs checks after editing files under `docs/`.
