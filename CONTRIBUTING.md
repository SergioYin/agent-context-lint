# Contributing

Thanks for improving **agent-context-lint**.

## Development

```bash
python -m pip install -e .
python -m pytest -q
```

## Pull request checklist

- Keep the project dependency-light.
- Add or update tests for behavior changes.
- Update README examples when CLI behavior changes.
- Do not commit secrets, private data, build artifacts, or cache files.
